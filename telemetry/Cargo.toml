[package]
name = "schema_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
zip = "0.6"
regex = "1"

tracing = "0.1"
tracing-subscriber = "0.3"
crossbeam-channel = "0.5"