[package]
name = "telemetry-journal"
version = "0.1.0"
edition = "2024"

[dependencies]
telemetry-core = { path = "../telemetry-core" }

chrono="0.4.44"
axum = "0.7"
tokio = { version = "1", features = ["full"] }