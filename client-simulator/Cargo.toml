[package]
name = "machine-simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
telemetry-core = { path = "../telemetry-core" }
postcard="1.1.3"
serde="1.0.228"
rand = "0.8"

reqwest = { version = "0.12", features = ["blocking"] }